{
  "name": "et-backend",
  "version": "1.0.0",
  "description": "backend for expense tracker",
  "main": "index.js",
  "scripts": {
    "start": "json-server database/db.json --port $PORT --no-cors",
    "start:dev": "json-server --watch database/db.json --port 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Manoshri Chattopadhyay",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
